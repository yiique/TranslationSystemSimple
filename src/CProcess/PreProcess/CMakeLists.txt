INCLUDE_DIRECTORIES(../../)
INCLUDE_DIRECTORIES(../)
AUX_SOURCE_DIRECTORY(. PREPROCESS_PROCESS_SRC_LIST)
ADD_LIBRARY(lib_preprocess_process ${PREPROCESS_PROCESS_SRC_LIST})
TARGET_LINK_LIBRARIES(lib_preprocess_process lib_tinyxml)

